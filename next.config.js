/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // 如需静态导出到 GitHub Pages，取消下面注释：
  // output: 'export',
  // distDir: 'dist',
  // images: { unoptimized: true },
}

module.exports = nextConfig
